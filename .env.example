# Copy this file to .env.local and fill in DATABASE_URL.
#
# Local dev (Docker Postgres):
#   postgresql://postgres:postgres@localhost:5432/decisionbroker
#
# Neon (deployment): use the POOLED connection string from the Neon dashboard,
# which includes ?sslmode=require. Set it in Vercel's env vars too.
DATABASE_URL=
